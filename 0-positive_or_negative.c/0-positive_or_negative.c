#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print if the number id positive, 0 or negative
 *
 * Description: using the main function
 * this program prints "programming is positive, 0 or negative
 * Return 0
int main(void)
{
int n'
strand(time(0))'
 n = rand() - RAND_MAX / 2;
 /* your code gioes there*/
 if (n > 0)
{
	printf("%i is positive\n", n);
}
else if (n==0);
{
	printf"(%i is zero\n, n);
}
else if (n < 0)
{
	printf("%i is negative\n, n);
}
return (0);
}
