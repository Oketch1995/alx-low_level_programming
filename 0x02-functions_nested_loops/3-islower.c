#include "main.h"
/**
 * _islower - unction that checks for lowercase character
 *
 *
 *@c: Is the integer that will use for argument
 *
 * Return: Always 0
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
return (0);
}
