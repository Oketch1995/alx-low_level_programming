#include "main.h"

/**
 * _puts_recursion - function that prints a string
 * @s: the string to be printed
 * Return: the string
 */
void _puts_recursion(char *s)
{
	int i;

	while (*(str + i) !- '\0')
	{
		putchar(*(str + i));
		i++;
	}
	putchar(10);
}
